spring:
  application:
    name: 'eureka-service-registry'
server:
  port: 8761
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF
management:
  endpoint:
    health:
      group:
        details:
          show-components: ALWAYS
          show-details: ALWAYS
          include:
            - ping
    shutdown:
      enabled: false
  endpoints:
    web:
      exposure:
        include:
          - '*'