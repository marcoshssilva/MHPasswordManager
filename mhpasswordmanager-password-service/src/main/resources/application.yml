gateway.allowed.configserver.code: ${GATEWAY_ALLOWED_CONFIGSERVER_CODE:changeit}
server:
  port: 12012
  servlet:
    context-path: /mypass-manager/passwords
spring:
  profiles:
    default: configserver
  application:
    name: password-service
  cloud:
    config:
      enabled: false
  servlet:
    multipart:
      max-file-size: 96MB
      max-request-size: 110MB
      resolve-lazily: false
  session:
    store-type: none
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:eureka@localhost:8761/eureka
  instance:
    prefer-ip-address: true
springdoc:
  version: @project.version@
  title: @project.name@
  description: @project.description@
  license-name: 'MIT'
  license-url: 'https://github.com/marcoshssilva/MHPasswordManager/blob/main/LICENSE'