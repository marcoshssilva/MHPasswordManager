spring:
  application:
    name: 'authorization-server'
  h2:
    console:
      enabled: true
      path: /h2

server:
  port: ${PORT:9000}
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
