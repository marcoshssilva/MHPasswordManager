spring:
  application:
    name: 'mhpasswordmanager-oauth2-authorizationserver'
  h2:
    console:
      enabled: true
      path: /h2

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 9000

security:
  client-id: system-admin
  client-secret: mysecret
